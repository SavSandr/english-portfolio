import styled from "styled-components";

const Main = styled.main`
    margin-top: 3rem;
    padding-top: 4.5rem;
    flex: 1;
    display: flex;
    width: 100%;
    flex-flow: column;
    position: relative;
`;

export default Main;
